# Публичный адрес, который попадёт в QR-код на production-сборке.
# Для локальной проверки создайте .env.local с LAN-адресом своего компьютера;
# .env.local не должен попадать в Git.
VITE_ISKIN_QR_BASE_URL=https://krasnotsarstvo.ru
